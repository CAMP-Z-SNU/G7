#include<stdio.h>
int main()
 {

    int A, B, product;
    printf("Enter two numbers: ");
    scanf("%d %d", &A, &B);
    product = A * B;
    printf("product: %d", product);
    return 0;
}